#pragma once

#include <iostream>
#include <cmath>
#include <cstdlib>

#include "graph.h"

class Landmark{
    public:
        Landmark(Graph g);
        Graph graph;
        int node_count;
        void BFS(airport1, airport2);
        void Landmark::printLandmarkPath(path);
        void Landmark::landmark(source, landmark, destination)
};